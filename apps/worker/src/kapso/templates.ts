/**
 * The Vorare template catalog lives in @wa/shared (shared with the web app's
 * reopen flow); this module re-exports it for the worker's kapso/* modules.
 */
export * from "@wa/shared/wa-templates";
